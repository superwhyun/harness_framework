이 프로젝트의 canonical 하네스 문서는 `/docs/HARNESS.md` 이다.

반드시 아래 순서로 진행하라.

1. `/AGENTS.md`
2. `/docs/HARNESS.md`
3. 관련 `docs/*.md`
4. 현재 `phases/` 파일

그 다음 `/docs/HARNESS.md` 에 정의된 step 기반 워크플로우를 그대로 따른다.

사용자가 추가 인자를 함께 준 경우, 그 요구도 같이 반영하라.
